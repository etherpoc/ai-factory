# TODO: このスタック固有の実装規約をここに書く

# 例 (phaser3):

# - 各シーンは `src/scenes/<Name>Scene.ts` に `class extends Phaser.Scene`

# - init/preload/create/update のライフサイクルを使い分ける

# - アセットは `public/` 配下、相対パスで `load.image()` する
