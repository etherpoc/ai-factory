  # 仕様書: 2048

  ## コンセプト
  4×4 グリッドの数字合体パズル「2048」のブラウザ動作版。
  キーボード操作、ミニマル UI。Phaser 3 + TypeScript 実装。

  ## 機能要件 (MUST)

  ### コアパズル
  - 4×4 グリッド
  - 操作: 矢印キー / WASD（上下左右スライド）
  - タイル生成: スライドでタイルが1枚以上動いた場合のみ、空きマスに「2（90%）」または「4（10%）」をランダム配置
  - 合体: 同じ数字が隣接した状態でスライドすると合算（2+2=4, 4+4=8, ...）
  - ゲームオーバー: 盤面が全て埋まり、どの方向にスライドしても合体不可能な状態
  - クリア: 2048 タイル生成でクリア画面へ

  ### スコア
  - スコア = 合体で生成されたタイルの数値合計（累積）
  - 画面上部に現在のスコアを表示

  ### シーン構成
  - Title: タイトル画面（Start ボタン）
  - Game: メインゲーム（グリッド + スコア）
  - GameOver: 最終スコア表示 + Retry ボタン
  - Clear: 2048 到達時の祝福画面 + Continue / Retry

  ### 非機能要件
  - 60 FPS 維持
  - 1 スライド処理 < 100ms（アニメーション込み）

  ## 除外（後で iterate で追加）
  - コンボシステム
  - ショップフェーズ
  - アーティファクト
  - コンシューマブル
  - ボスブラインド
  - 特殊タイル（ガラス / 黄金）
  - localStorage 永続化

  ポイント:
  - spec 冒頭で「Phaser 3 + TypeScript」を明記（recipe と技術スタック一致）
  - 「除外」セクションを明示 → interviewer が余計な質問しない
  - 非機能要件で性能目標を明示（前回落ちた原因への予防）
